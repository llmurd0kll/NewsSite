# 📰 NewsSite

**NewsSite** — это демонстрационный проект(тестовое) новостного портала на **ASP.NET Core MVC** с поддержкой локализации (RU/EN), системой аутентификации и административной панелью для управления новостями.

---

## 🚀 Возможности

- **Публичная часть**
  - Просмотр списка новостей с пагинацией (AJAX "Load more")
  - Детальная страница новости
  - Переключение языка (RU/EN)
  - SEO‑мета‑теги и динамические заголовки

- **Админка**
  - CRUD для новостей (создание, редактирование, удаление)
  - Загрузка изображений
  - Управление статусом публикации
  - Авторизация по ролям (Admin/User)

- **Аутентификация**
  - Регистрация и вход пользователей
  - Валидация форм с локализованными сообщениями
  - Поддержка `Remember me`

- **Локализация**
  - Централизованные ресурсные файлы `SharedResource.resx` и `SharedResource.en.resx`
  - Все UI‑строки вынесены в ресурсы
  - Поддержка `IStringLocalizer<SharedResource>` во всех вьюхах

---

## 🛠️ Технологии

- **ASP.NET Core MVC 8**
- **Entity Framework Core** (PostgreSQL)
- **ASP.NET Identity**
- **Razor Views**
- **HTML + CSS**
- **jQuery + AJAX**
- **Localization (IStringLocalizer)**

---

## 📂 Архитектура проекта

- **NewsSite.Domain/**
  - Сущности доменной модели
  - Бизнес‑логика
  - Контракты и интерфейсы

- **NewsSite.Web/**
  - Веб‑приложение на ASP.NET Core MVC  
  - **Areas/Admin/**
    - `Controllers/` — контроллеры админки (CRUD новостей)  
    - `Views/` — Razor‑вьюхи админки  
  - **Controllers/** — публичные контроллеры (Home, News, Account)  
  - **Models/** — ViewModel’и и InputModel’и  
  - **Views/**  
    - `Home/` — главная страница, Privacy  
    - `News/` — список новостей, детали, partial‑вьюхи  
    - `Account/` — Login, Register  
    - `Shared/` — Layout, общие partial‑вьюхи  
  - **Resources/** — централизованные ресурсные файлы (`SharedResource.resx`, `SharedResource.en.resx`)  
  - **wwwroot/** — статические файлы (CSS, JS, изображения)  
  - **Program.cs** — точка входа, настройка сервисов и middleware  

- **README.md** — документация проекта  

---

## ⚙️ Настройка и запуск

1. Клонировать репозиторий:
   ```bash
   git clone https://github.com/username/NewsSite.git
   cd NewsSite
   ```
2. Настроить подключение к PostgreSQL в appsettings.json:
    ```json
    "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Database=newssite;Username=postgres;Password=yourpassword"
    }
    ```
3. Применить миграции:
    ```bash
    dotnet ef database update
    ```
4. Запустить проект:
    ```
    dotnet run --project NewsSite.Web
    ```
## 🌐 Локализация

- Базовый язык: **русский** (`SharedResource.resx`)
- Английский: **SharedResource.en.resx**
- Переключение языка: ссылки `RU/EN` в шапке сайта
- Все строки UI вынесены в ресурсы (включая меню, кнопки, валидацию, админку)
- Используется `IStringLocalizer<SharedResource>` для централизованного доступа к переводам

---

## 📜 Лицензия

MIT License
